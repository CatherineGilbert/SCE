- update documentation for these changes

/// added warning about SSURGO not functioning
/// added timeline plots, which are still a work in progress
/// added Sheila to the credits
/// added a more informative label to the first tt/precip section
/// removed some useless files
/// users can now return to previous analyses (as long as the files are still there). also, no more flipping between user and debug versions for testing analysis features. 

- change the timeline plots to use maturity group instead of "genetics"
- maybe add the I lines on the timeline back in, so people can see start and end in planting and harvest?

- what is up with total senesence in maize :[[
- make it so that if no soils are created the sim fails immediately
- make it so that the soil module replacement simply does not create a file if the soil profile cannot be found. then fix the pH in those template models. people want to run the template models by themselves sometimes. 

- fix the UI on the latitude and longitude boxes in "create gridded inputs"
- fix text on the "configure pheno periods" page
- find where the NaNs in the seasonal_data are coming from and if they are causing problems
- break the ID_corr function into different functions for computation and plot rendering, so changing text size isn't such a pain. 
  
- check that the soy template models match the pheno that's in the recalibration models (they do not)

- sheila's lovely contributions
  - add 3D plotly plots for maturity date, planting date, and genetics
  - add timeline plots
      - enable text scaling on download plots
  - look into other sheila plots and code
  - the seasonal analogues report should be moved to sheila's tab probably 

- have the choice of what variables are (mean / sum / max / min) be something that can be changed in the interface, have the summed variables from the tempate files be recognized as summed by default
- change buffer from two weeks default to something customizable
- add direct controls for ReaperMan and Sowing scripts
 - toggle for frost date

- add search bar to seasonal covariates selector

- turn things into functions instead of one chunk of code
- have maturity handling autoselect as appropriate when template model is chosen
- when matrices are downloaded, have them have columns corresponding to the columns of the nametag
- add loading symbols for graphs
- add error message when graphs can't load because only a single record is being used

  ??? have the period_key creation read phases from the APSIM model internals directly rather than infer them from the outputs
  these values aren't stored in the template simulations unfortunately
  