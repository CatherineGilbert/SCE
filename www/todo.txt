- maybe do something with the envirotyping stuff in the testing script?? but who knows how that would be integrated. 
  - create a section that says "season", move current season sim tab and season heatmap tab there
  - move dendro, PCA, and PCA-colored map into new page that tabs out from under the seasonal similarity matrix tab


!! fix date parsing on inputs and make sure they're correctly documented

- update documentation for these changes
- turn things into functions instead of one chunk of code when you can
!! push changes to online version

- fix the issue with working directory in the app. currently reloading it seems to break things
  
- timeline plots
  - ability to filter year, maturity group, site
  - add text to timeline plots
  - change the timeline plots to use maturity group instead of "genetics"
  - maybe add the I lines on the timeline back in, so people can see start and end in planting and harvest?
  - figure out how I want to be aggregating this data
  - make it so when you mouse over a point it tells you the date. 

- soils
  - rerun soils with rounded coordinates if they fail a first time?
  - test Australian soils
  - make it so that the soil module replacement simply does not create a file if the soil profile cannot be found. then fix the pH in those template models. people want to run the template models by themselves sometimes. 
         - attach an empty soil profile to the template model the first time it is loaded for copying
  
- errors / progress log
  - have error messages from apsimx script, which are captured by future as err$message, save and display directly to the progress log
  - remove error messages based on active folder counting 
  - ???? when running trials after another trial is run, the "simulations finished" and "successful simulations" are not cleared out when the sim restarts

- advanced controls tab
  - make it look nice
  - add direct controls for ReaperMan and Sowing scripts
   - toggle for frost date
  - have maturity handling autoselect as appropriate when template model is chosen

- seasonal summation controls
  - fix text on the "configure pheno periods" page
    ??? have the period_key creation read phases from the APSIM model internals directly rather than infer them from the outputs ??? these values aren't stored in the template simulations unfortunately
  - have the choice of what variables are (mean / sum / max / min) be something that can be changed in the interface, have the summed variables from the tempate files be recognized as summed by default
  - period should be number-sorted on table
  
- seasonal similarity matrix page
  - ???? please fix the issue where the where the first maturity's seasonal heatmap of the soybean example input doesn't display
  - add search bar to seasonal covariates selector
  - add loading symbols for graphs
  - add error message when graphs can't load because only a single record is being used
  - ??? a PCA of some kind?
  - add button to cluster or uncluster heatmap
  
- seasonal heatmaps page
  - fix redundant text
  - add button to cluster or uncluster heatmap
  